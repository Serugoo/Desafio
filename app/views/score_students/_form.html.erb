<%= form_for(@score_student) do |f| %>
  <% if @score_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@score_student.errors.count, "error") %> prohibited this score_student from being saved:</h2>

      <ul>
      <% @score_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :enade_id %><br>
    <%= collection_select(:score_student, :enade_id, @Enades_List, :id, :year)%>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= collection_select(:score_student, :student_id, @Students_List, :id, :name)%>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.text_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
